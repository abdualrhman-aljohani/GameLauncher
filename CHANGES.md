# سجل التعديلات (مراجعة وإصلاح)

> الهدف: إصلاح البقات والثغرات **بدون تغيير الشكل ولا سرعة التشغيل**.
> ⚠️ هذه التعديلات **لم تُبنَ بـ MSVC** (بيئة المراجعة لا تحتوي Windows). المنطق الجديد اختُبر على Linux
> (انظر "ما اختُبر")، لكن لازم تبني المشروع وتمرّ على قائمة الاختبار في آخر الملف.

---

## 1) حفظ البيانات والترميز

| الملف | التعديل |
|---|---|
| `Common/Utf8Stream.h` (جديد) | `Utf8In` / `Utf8Out`: بديل جاهز لـ `wifstream`/`wofstream`. يكتب UTF-8 دائماً (سابقاً `wofstream` بلغة "C" في MSVC يتوقف عند أول حرف عربي فتُقطع الملفات). يقرأ UTF-8 (مع/بدون BOM) وUTF-16LE، وإذا لم يكن UTF-8 صالحاً يقرأه كما كان يقرأ القديم (بايت = حرف) فلا تضيع إعدادات موجودة. يحافظ على `\n` ⇄ `\r\n`. |
| كل ملفات `.cpp` | استبدال `std::wifstream` → `Utf8In` و`std::wofstream` → `Utf8Out` (68 موضع). الباقي بدون تغيير. |
| `Config.cpp` `SaveGames` | يتحقق من نجاح الكتابة قبل الاستبدال (كان يستبدل الملف السليم بملف ناقص)، تثبيت على القرص، ونسخة `games.cfg.bak` من آخر إصدار سليم. |
| `Config.cpp` `CfgField` | أي فاصل/سطر جديد داخل اسم أو مسار أو args يُستبدل بمسافة بدل ما يخرّب بنية `games.cfg`. |
| `Panel.cpp` `SaveGamesSafe` | الـ Panel يدمج (بالأكبر) وقت اللعب/عدد المرات/آخر لعب من القرص قبل الحفظ، حتى لا يكتب فوق إحصائيات حدّثها المحرك. تصفير الإحصائيات والاستيراد يستخدمان الحفظ العادي عمداً. |
| `Panel.cpp` استيراد | يحفظ `games.cfg.before-import.bak` قبل استبدال القائمة. |
| `Panel.cpp` تصدير | لا يترك ملفاً ناقصاً إذا فشلت الكتابة. |
| `PerformanceMonitor.cpp` CSV | BOM في أول الملف ليفتح Excel الأسماء العربية صحيحة. |

## 2) محلل JSON (`Panel.cpp`)

استُبدل البحث النصي الهش بمحلل يحترم النصوص والتداخل:
- مسار مثل `D:\Games\[Repack]\x.exe` لم يعد يقطع مصفوفة `paths` (سحب وإفلات عدة ملفات).
- قيمة تطابق اسم مفتاح (مثل `"action":"name"`) لم تعد تلخبط القراءة.
- مفاتيح مكررة داخل كائنات متداخلة (الرفاق `companions`) لا تتداخل مع مفاتيح اللعبة.
- إضافة `\b` و`\f` لفك الهروب.

## 3) الواجهة (`panel_ui.js`) — بدون أي تغيير في الشكل

- زر الحذف (اللعبة والرفيق) كان ينكسر مع أي اسم فيه `'` (مثل *Assassin's Creed*) ويسمح بحقن JS. أُصلح بـ `data-name` + `this.dataset.name`.
- مفتاحا الترجمة الناقصان `suggestExeMessage` و`suggestExeAdd` أُضيفا (كانا يظهران نصاً خاماً).
- ApexCharts ثُبّتت على `apexcharts@6` بدل "أحدث نسخة" (يمنع تحديثاً مفاجئاً لإصدار رئيسي جديد).

## 4) تحصين WebView2 والتحميل

- `NavigationStarting`: أي تنقّل خارج الصفحة المحلية يُلغى (روابط http/https تُفتح في المتصفح). `NewWindowRequested`: ممنوع.
- الرسائل من الواجهة تُقبل فقط إذا مصدرها `file:///`.
- `put_AreDevToolsEnabled(FALSE)` في **Release فقط** (Debug يبقى للتطوير).
- `nvml.dll` و`xinput*.dll` تُحمّل بـ `LOAD_LIBRARY_SEARCH_SYSTEM32` (لا يبحث في مجلد البرنامج).
- `Launcher.vcxproj`: `BufferSecurityCheck` من `false` إلى `true` (كان `/GS-` يناقض `SDLCheck`).
- استخراج ملفات الواجهة: يرجع خطأ لو فشلت الكتابة بدل صفحة فارغة.

## 5) التشغيل والـ Boost (`Launcher.cpp`, `EtwFps.cpp`)

- `LaunchPath` صار يرجع `bool`: فشل `ShellExecuteEx` (ملف غير موجود / إلغاء UAC) **لا يُحسب مرة لعب** ولا تُسجَّل اللعبة كأنها شغّالة.
- MMCSS: قيم `BoostBackup` الافتراضية صارت قيم ويندوز الأصلية (`2 / Medium / Normal`) بدل قيم الـ Boost نفسها؛ و`RestoreMmcssValues` يتحقق من النتيجة ولا يمسح النسخة الاحتياطية إذا فشل.
- أولوية **Realtime**: أُخفيت من القائمة (5 خيارات)، وأي قيمة قديمة تُعامل High، والتطبيق نفسه يمنعها (قد تجمّد النظام).
- ETW: رفض الصلاحيات لا يعيد المحاولة (كان يؤخر التشغيل ~ثانية بدون فائدة).
- سجل الأولوية/الـ affinity يكتب hex فعلاً (كان يكتب رقماً عشرياً بعد `0x`).

## 6) المشروع والوثائق

- حُذف `Panel/Panel.vcxproj` و`Panel/Panel.rc` و`Panel/packages.config` (مشروع ميت خارج الـ `.sln` ولا يُربَط أصلاً؛ الـ Panel مبني داخل `Launcher.vcxproj`).
- `Launcher.rc`: الإصدار 1.7.0.0 وتصحيح نص الترخيص (كان "All Rights Reserved" مع PolyForm NC).
- `.gitignore` جديد.
- `README.md` (الإنجليزي) كان **تالفاً** (كل سطر عنوان + علامات escape) — أُعيد بناؤه.
- تصحيح الأرقام: الواجهة فيها **6 أنماط رسم و6 ثيمات** (+ مخصص)، لا 13 و33. توحيد حد ويندوز الأدنى (2004+). تخفيف ادعاء "آمن 100%" مع Anti-Cheat. توضيح أن لوحة التحكم تحتاج إنترنت.

---

## 8) ارتفاع الذاكرة بعد اللعب (18–19 MB) — تقليم الذاكرة

**التشخيص (من قراءة الكود، ما قست الذاكرة فعلياً):** الفرق بين لعبة بـ Boost ولعبة بدون Boost في `LaunchGame` هو:
- **الـ Overlay**: يظهر عند التشغيل فقط إذا اللعبة **بدون** Boost (`showOnGameLaunch && !boostFps`).
- **الـ Black Box**: لا يبدأ إذا اللعبة فيها Boost مع "إيقاف الإحصائيات".
وعند انتهاء اللعبة يُوقَف الـ Black Box، لكن **الـ Overlay لا يُخفى تلقائياً** (يبقى حياً بمؤقت كل 500ms حتى تخفيه بنفسك)، والذاكرة التي استُخدمت وانتهت تبقى محسوبة للعملية حتى يقلّمها ويندوز، وهذا ما يظهر لك ثابتاً على 18–19 MB.

**الإصلاح:** `RunMemoryTrim()` تستدعي `EmptyWorkingSet` بعد: إغلاق الدائرة (10 ثوانٍ)، انتهاء آخر لعبة (5 ثوانٍ)، إخفاء الـ Overlay (5 ثوانٍ). لا يقلّم والدائرة مفتوحة أو مراقبة الأداء شغّالة. **لا يحذف أي بيانات ولا الكاش** (ما يلزم يرجع تلقائياً عند الحاجة)، ولا يؤثر على سرعة فتح الدائرة (المؤقت مؤجَّل).
**ما تحقّقت منه:** الأثر الفعلي على الرقم في مدير المهام (ما شغّلت شيء على Windows).

## 9) تنبيه حرارة المعالج

ميزة حرارة المعالج غير مدعومة أصلاً (الكود يحفظ الخيار فقط ولا يقرأ أي حساس، وما فيه زر لتفعيلها). التنبيه كان يظهر تحت "مراقبة الأداء" عند التشغيل بصلاحيات مسؤول → حُذف (HTML + الترجمة + الشرط في JS). تنبيه "يحتاج صلاحيات مسؤول" (أحمر) لم يتغيّر.
بقايا خاملة غير ظاهرة تُركت عمداً: بطاقات/رسم حرارة المعالج في التقرير (تظهر فقط لو وُجدت قيم، ولا توجد)، وحقل `performanceCpuTemp` في `games.cfg`.

## 10) نتيجة زر "استرجاع إعدادات Boost"

الزر كان يقول "جاري الاسترجاع" فقط. الآن المحرك يرجّع نتيجة (`SendMessageTimeout`) وتظهر في شريط الحالة:
تم الاسترجاع / لا شيء لاسترجاعه / **فشل — شغّل المحرك كمسؤول** / تعذّر الوصول للمحرك (يعمل بصلاحيات أعلى). النجاح = لم يبقَ شيء في النسخة الاحتياطية.

## 11) شريط العنوان الداكن

`DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)` بعد إنشاء نافذة اللوحة. على ويندوز أقدم من 10 2004 يُتجاهل بصمت. (تغيير في الشكل: مطلوب منك.)

## 12) وضع الحدّة على الشاشات المكبّرة (اختياري، مطفي افتراضياً)

- خيار جديد في الإعدادات: **"وضع الحدّة على الشاشات المكبّرة"** (يُحفظ في `sharp_mode.cfg`، ويُطبَّق بعد إعادة تشغيل البرنامج).
- **مطفي = السلوك الأصلي حرفياً** (`GetDpiScale()` يرجع 1.0 وكل الحسابات بنفس الأرقام القديمة).
- مفعّل = `SetProcessDPIAware()` + رسم بـ `ScaleTransform` + تحويل إحداثيات الماوس + أحجام النوافذ × المعامل.
- **تشخيص:** عند التفعيل يكتب `launcher.log` سطر `[Sharp]` كل مرة تفتح الدائرة (المعامل، أبعاد الشاشة، الأحجام). لو ظهرت الدائرة كبيرة، أرسل هذا السطر ونعرف السبب بالأرقام.
- **تحذير:** هذا نفس كود التجربة التي كبّرت الدائرة على لابتوب 150%؛ لم أعرف سببها. لذلك هو تجريبي ومطفي.

## 13) الواجهة بدون إنترنت (#7)

- `tools/build-ui-assets.ps1`: ينزّل مرة واحدة إلى `ui_assets` (بجانب الـ exe): **نفس سكربت Tailwind Play CDN** (فلا يتغير الشكل بسبب classes ديناميكية)، Font Awesome بخطوطه مدمجة، خطوط Cairo/Tajawal/JetBrains Mono (العربي + اللاتيني)، ApexCharts 6.x، مع `VERSIONS.txt` وبصمات SHA256. يبني في مجلد مؤقت ولا يستبدل شيئاً إلا بعد نجاح كل الخطوات.
- `panel_ui.html/js`: يحمّل من `ui_assets` إن وُجد، **وإلا يسقط تلقائياً على الـ CDN كما كان** (لا شيء ينكسر لو ما شغّلت السكربت).
- `Panel.cpp`: استبدال `__GL_ASSETS__` بعنوان `file:///…/ui_assets/` (مشفّر) أثناء استخراج الواجهة.
- **ما اختُبر:** السكربت **لم يُشغَّل** (لا PowerShell في بيئة المراجعة)؛ اختُبر فقط منطق التعابير النمطية على عيّنات مطابقة لشكل ملفات cdnjs وGoogle Fonts. شغّله وقل لي إن فشلت خطوة.
- **اختبار:** شغّل السكربت، أعد التشغيل، افصل الإنترنت وافتح اللوحة: نفس الشكل والأيقونات والخطوط والرسوم. لو فيه فرق احذف `ui_assets` فيرجع للـ CDN.

## 14) المثبّت (#11)

- `installer/GameLauncher.iss` (Inno Setup 6.3+): تثبيت في Program Files، اختصارات، فحص WebView2، ترقية فوق القديم (`AppId` ثابت)، إغلاق البرنامج إن كان شغّالاً (`AppMutex`)، تشغيل البرنامج بعد التثبيت بصلاحيات عادية (`runasoriginaluser`).
- **الإزالة:** تستدعي `GameLauncher.exe --uninstall-cleanup` (خيار جديد في `Main.cpp`) الذي يرجّع قيم Boost في registry ويحذف التشغيل التلقائي (المهمة المجدولة + مفتاح Run) ثم يخرج بدون أي نافذة. بعدها تسأل إن كنت تريد حذف `%APPDATA%\GameLauncher` (الافتراضي: لا).
- **ما اختُبر:** لا شيء (لا Inno Setup ولا Windows عندي). السكربت مكتوب حسب توثيق Inno. **ابنِه وجرّب:** تثبيت، ترقية فوق نسخة قديمة، إزالة.
- لم أوقّع الملف رقمياً (`SignTool` معطّل في السكربت): بدون توقيع قد يظهر تحذير SmartScreen.

## 15) استيراد الألعاب من المتاجر (زر "استيراد الألعاب")

- زر جديد في رأس قسم الألعاب (المكان الذي حدّدته بالمربعات الحمراء) يفتح قائمة بألعابك المثبّتة: **Steam** (يقرأ `libraryfolders.vdf` وكل `appmanifest_*.acf` في كل المكتبات)، **Epic** (ملفات `Manifests\*.item`)، **GOG Galaxy** و**Ubisoft Connect** (من registry). يقرأ ملفات محلية فقط: لا إنترنت ولا تسجيل دخول.
- تُستبعد أدوات Steam (Proton، Steam Linux Runtime، Steamworks Redistributables) والتثبيتات الناقصة وإضافات Epic (DLC).
- الألعاب الموجودة عندك أصلاً تظهر معتّمة بشارة "مضافة" ولا تتكرر (حتى لو أضفتها سابقاً بمسار الـ exe).
- **طريقة التشغيل:** Steam بـ `steam://rungameid/<id>`، Epic بـ `com.epicgames.launcher://apps/...`، Ubisoft بـ `uplay://launch/<id>/0` (تمرّ عبر المتجر فتعمل التحديثات وتسجيل الدخول)، GOG بمسار الـ exe مباشرة. الأيقونة تؤخذ من ملف اللعبة.
- **غير مغطّى:** EA app، Battle.net، Rockstar، Xbox/Microsoft Store — استعمل "فحص مجلد" أو السحب.
- **ما اختُبر:** محللات ملفات المتاجر (VDF/JSON) اختُبرت على عيّنات بأشكال Steam/Epic الحقيقية + 100 ألف مدخل عشوائي؛ فحص نحوي لبقية الكود. **لم يُشغَّل على Windows**: أول تجربة على جهازك هي الاختبار الحقيقي (خصوصاً مسارات registry لـ GOG وUbisoft).

## 16) سحب المجلدات + زر "فحص مجلد"

- **السبب القديم:** البحث كان يقف عند أول 80 ملفاً، فمجلد مثل `steamapps\common` تستهلك أول لعبتين منه كل النتائج ولا تُفحص بقية المجلدات.
- **الآن:** إذا كان المجلد "مكتبة" (فيه مجلدات ألعاب متعددة) يُؤخذ **أفضل ملف تشغيل لكل مجلد فرعي** ويُسمّى باسم المجلد، وكلها تظهر مؤشّرة مسبقاً. إذا أفلتّ مجلد Steam أو `steamapps` يدخل مباشرة إلى `steamapps\common`. مجلد لعبة واحدة يعمل كما كان.
- حدود أمان: 400 مجلد فرعي، و8 ثوانٍ كحد أقصى للفحص، وتجاهل الروابط الرمزية.
- زر **"فحص مجلد"** يفتح نافذة اختيار مجلد ويستخدم نفس المنطق.
- **أسماء أذكى:** ملفات Unreal مثل `Endeavor-Win64-Shipping.exe` تُسمّى من مجلد اللعبة ("Aliens Fireteam Elite")؛ ينطبق على الإضافات الجديدة فقط (ألعابك الحالية لا تتغير).

## 17) وضوح السحب والإفلات للمستخدم الجديد

- عند سحب ملفات فوق النافذة تظهر طبقة كاملة: "أفلت هنا لإضافة اللعبة".
- عندما تكون القائمة فارغة تظهر بطاقة كبيرة تشرح السحب والأزرار.

## 18) المتصفحات وEpic Launcher (UWP)

- **السبب (من الكود):** اختصارات برامج سطح المكتب (Chrome / Edge / Epic Launcher) قد تحمل خاصية AppUserModel.ID بسيطة مثل "Chrome"، وكان البرنامج يعاملها UWP (`uwp:Chrome`) ويشغّلها بـ `ActivateApplication` التي تعمل فقط مع تطبيقات الحزم، فتفشل بصمت. يتوقف ذلك على الاختصار، لذلك يعمل عندك ولا يعمل عند صديقك.
- **الإصلاح:** (أ) AUMID بلا "!" مع وجود ملف تنفيذي في الاختصار يُعامل برنامج عادي (يُشغَّل من الـ exe). (ب) لو فشل التنشيط يُجرَّب `shell:AppsFolder\<AUMID>` الذي يشغّل النوعين، ويصلح **الإدخالات القديمة** المحفوظة بـ `uwp:` بدون إعادة إضافتها.
- **ما اختُبر:** لم أستطع اختباره (لا جهاز صديقك). تأكد معه أن Chrome/Edge/Epic تفتح بعد التحديث، وإن لم تفتح أرسل لي محتوى `launcher.log` (سطر `Launch FAILED`).

## 19) أول تشغيل: اللوحة مع الدائرة

- لمستخدم جديد فقط (لا يوجد `first_run_done.txt` ولا `welcome_shown.txt`) تُفتح اللوحة بعد ~1 ثانية ثم تظهر الدائرة فوقها بعد ~2 ثانية (بتلميحات أول مرة). يُسجَّل `welcome_shown.txt` فلا يتكرر. المستخدمون الحاليون لا يتأثرون.

## 20) النسخة الاحتياطية والإحصائيات

- تحققت برمجياً أن **كل حقول الإحصائيات** (`totalPlaySeconds`، `playCount`، `lastPlayedUnix`) تُصدَّر وتُستورَد (فحص تطابق آلي: 28 حقلاً مصدَّراً = 28 مستورداً).
- الناقص كان **سجل الجلسات** (قائمة الجلسات لكل لعبة، محفوظة في ملفات منفصلة): صار يُحفظ في النسخة (آخر 1000 جلسة لكل لعبة) ويُستعاد عند الاستيراد. النسخ القديمة بلا سجل تترك سجلك الحالي كما هو.
- **غير مشمول:** تقارير الأداء (Black Box) — ملفات كبيرة منفصلة.

## اختبارات جديدة
- `tests/`: `test_json.cpp` و`test_import.cpp` (تُبنى بأي مترجم، بدون Windows). انظر `tests/README.md`.
- نقلتُ محلل JSON من `Panel.cpp` إلى `Common/MiniJson.h` (بدون تغيير في منطقه) ليُستعمل في قراءة ملفات Epic ويُختبر منفصلاً.

## 21) إعادة تصميم زر "استيراد الألعاب" و"فحص مجلد"

- يبقيان **ظاهرين** في رأس قسم الألعاب، وبنفس ارتفاع زر "إضافة لعبة" (كان أقصر بوضوح).
- ثلاث درجات أهمية: **إضافة لعبة** (ممتلئ كما هو) ← **استيراد الألعاب** (تدرّج ولون الثيم وحدّ مضيء وأيقونة داخل بلاطة ملوّنة، وتحته سطر صغير "Steam · Epic · GOG · Ubisoft") ← **فحص مجلد** (زجاجي هادئ وتحته "يشمل المجلدات الفرعية").
- التغيير في `panel_ui.html` + كتلة CSS جديدة `.hdr-btn` في `panel_ui.css` (لا تعتمد على `backup-btn` حتى لا يلغيها وضع الزجاج) + مفتاح ترجمة `scanFolderSub`. لا يوجد أي تغيير في C++.
- **ما اختُبر:** لم أرَ الشكل مرسوماً (لا متصفح في بيئتي). تأكد منه في: الثيمات الستة، ووضع الزجاج، والإنجليزية. لو تبي تعديل أرقام (الحجم، الشفافية، الحدّ) فكلها في كتلة `.hdr-btn` في آخر قواعد `.backup-btn`.

## 22) ملاحظات صديقك (شرح الأيقونات، الألعاب ذات اللانشر، وتحسينات صغيرة)

1. **الألعاب خارج المتاجر (Genshin وأمثالها + مجلد Games):** زر "استيراد الألعاب" يفحص الآن أيضاً مجلدات شائعة على **كل الأقراص الثابتة**: `\Games` و`\Game` و`\GOG Games` وألعاب اللانشرات الخاصة: `\Program Files\HoYoPlay\games` و`\Genshin Impact` و`\Star Rail` و`\Wuthering Waves` (مع نسخ `Program Files` منها). يُؤخذ أفضل ملف تشغيل لكل لعبة (يفضّل ملف اللعبة الكبير على `launcher.exe`)، ويُحذف من الاسم اللاحق " game" (مثل "Genshin Impact game"). تظهر بشارة **Games**، ولا تتكرر مع ألعاب Steam/Epic. سقف زمني 12 ثانية.
   - **ملاحظة:** مواقع تثبيت اللانشرات الأخرى (Riot وBattle.net وEA...) لم أدرجها؛ استعمل "فحص مجلد" أو السحب.
   - **ما اختُبر:** فحص نحوي فقط؛ مسارات miHoYo/HoYoPlay/Kuro مكتوبة من معرفتي العامة ولم أتحقق منها على جهاز فيه هذه الألعاب.
2. **شرح الأيقونات (tooltips):** كل أيقونات صف اللعبة صار لها شرح عند التمرير: المفضلة، الظهور في الدائرة (العين)، **الرقم الأخضر = عدد البرامج المرافقة** (كان يظنّه المستخدم عدد مرات اللعب)، فتح المجلد، التفاصيل، الحذف؛ وكذلك شارات UWP/Boost/الكتم/رقم الاختصار/الملف المفقود، وحذف البرنامج المرافق، وأزرار شريط الوصول السريع. عربي وإنجليزي.
3. **الوصول السريع:** يظهر فقط إذا للعبة برنامج مرافق واحد على الأقل.
4. **أيقونة لغة العربية** في "لغة اللعبة داخل التطبيق": بدل الكعبة صار الحرف **ع** (بنفس شكل حرف E).
5. **الخلفية الدائرية المخصصة:** أول ما تضيف صورة (لم تكن هناك صورة) يتفعّل تلقائياً "إخفاء أيقونة اللعبة الأصلية" ويقدر المستخدم يطفيه. عند **استبدال** صورة موجودة يبقى اختياره كما هو. (إزالة الخلفية تعيده لوضعه الافتراضي.)

## 23) إصلاح أسماء ألعاب مجلد Games في قائمة الاستيراد

- **الخلل:** مجلد مثل `C:\Games` فيه ملف تنفيذي مبعثر (`unlockfps_nc.exe`) كان يُعتبر "لعبة واحدة"، فتظهر كل مكتبتك كعنصر واحد اسمه **Games** (وتُسمّى `D:\HoYoPlay\games` "games"). السبب: كشف المكتبة يتوقف عند وجود ملف تنفيذي مباشر.
- **الإصلاح:** المجلدات المعروفة كمكتبات (`\Games` و`\Game` و`\GOG Games` و`\HoYoPlay\games`) تُعامل كمكتبة دائماً: كل مجلد فرعي = لعبة، وأي ملف تنفيذي مباشر بجانبها يُضاف لعبةً مستقلة. **الأسماء من اسم الملف التنفيذي** (نفس طريقة التطبيق عند السحب، مع تحسين ملفات Unreal). مجلدات اللعبة الواحدة (`\Genshin Impact` `\Wuthering Waves`...) تأخذ أفضل ملف تشغيل باسمه.
- **Spacewar** (تطبيق تجريبي من Valve) لم يعد يظهر ضمن ألعاب Steam.
- سطر "المصادر" في نافذة الاستيراد صار بفواصل (Steam · Epic · GOG ...) لأن الواو العربية كانت تلتصق بالأسماء اللاتينية وتظهر كحرف زائد.

## 24) أيقونة جديدة، إنجليزي افتراضي، ووثائق للنشر (الإصدار 1.8.0)

- **الأيقونة:** تصميم جديد (مربع داكن ناعم وحوله أيقونات ملوّنة تدور حول مركز تشغيل مضيء) بدل الشكل القديم. كل حجم (16 → 256) مرسوم على حدة ليبقى واضحاً في التراي وشريط المهام. `Common/app_icon.ico` (والقديمة محفوظة في `Common/icon_alternatives/app_icon_old.ico`، وبديل حلقي في `app_icon_B.ico`). مولّد الأيقونة: `tools/make_icon.py`. **لم أرَ الأيقونة داخل ويندوز فعلاً** (رأيتها في معاينة فقط) — تأكد منها في التراي وشريط المهام.
- **اللغة:** المستخدم **الجديد** يبدأ بالإنجليزية (تُحفظ فوراً في `language.cfg`)؛ من عنده إعدادات سابقة تبقى لغته عربية كما هي فلا تتغير عليه بعد التحديث. وأخفيتُ الصفحة لحظات حتى تصل اللغة من البرنامج فلا يظهر وميض عربي/RTL لمستخدم إنجليزي (مهلة أمان 1.5 ثانية).
- **الإصدار 1.8.0:** `Launcher.rc` و`APP_VERSION` و`installer/GameLauncher.iss` و README. (لو لا تريد رفع الرقم، أعده إلى 1.7.0 في هذه المواضع.)
- **README (إنجليزي وعربي) و`CHANGELOG.md`** محدّثة بكل الجديد، مع قسم البناء والتثبيت والخصوصية وتحذير SmartScreen.
- **GitHub:** `.github/workflows/build.yml` (بناء + اختبارات) و`release.yml` (عند رفع tag: يبني الـ exe والمثبّت ويرفقهما مع SHA-256). **لم يُختبرا** (لا GitHub عندي).
- **التوقيع الرقمي:** `tools/sign-release.ps1` (PFX أو شهادة مخزّنة/سحابية أو Azure Artifact Signing) + `docs/PUBLISHING.ar.md` (الخيارات المتاحة لك من السعودية ومتطلبات متجر مايكروسوفت). **Azure Artifact Signing غير متاح لك** (الأفراد أمريكا/كندا فقط). لا شهادة عندي لاختبار السكربت.

## تغييرات سلوك تستاهل علمك

1. خيار Realtime اختفى من قائمة أولوية العملية.
2. لعبة فشل تشغيلها لا تُحسب ولا تظهر "شغّالة".
3. ملفان جديدان بجانب الإعدادات: `games.cfg.bak` و`games.cfg.before-import.bak`.
4. `SaveGames` يضيف تثبيتاً على القرص (`FlushFileBuffers`) ونسخة `.bak` — بعد إقلاع اللعبة لا قبلها، فلا يؤخر تشغيلها.
5. F12 (DevTools) لا يعمل في Release.

## لم يُنفَّذ (وسببه)

| البند | السبب |
|---|---|
| تضمين Tailwind/FontAwesome/الخطوط محلياً (الواجهة تحتاج إنترنت) | لا شبكة في بيئة المراجعة، وتحويل Tailwind لنسخة مبنية قد يغيّر الشكل. الأفضل تسويه عندك وتقارن الشكل. |
| CSP / SRI | لا أقدر أجرّبها على WebView2؛ خطأ واحد يكسر الواجهة. |
| جعل وضع الحدّة (DPI) هو الافتراضي | النسخة التجريبية كبّرت الدائرة على لابتوب 1440p بتكبير 150%؛ لذلك صار خياراً **مطفياً افتراضياً** (بند 12) لحين تجربته. |
| نقل ملفات الواجهة من `%APPDATA%` القابل للكتابة | قد يكسر الخلفيات المخصصة (تعتمد على `file:///`). |
| نقل `WebView2Data` والأداء إلى LocalAppData | يضيّع كاش/إعدادات محفوظة داخل الواجهة. |
| ملف إعدادات واحد بدل 15+ ملف `.cfg` | تغيير كبير بلا فائدة تكفي المخاطرة. |
| نافذة تأكيد قبل الاستيراد / فحص `runAsAdmin` و`launchArgs` المستوردة | نافذة أصلية تختلف عن شكل اللوحة؛ استُعيض عنها بنسخة احتياطية صامتة. |

## ما اختُبر (على Linux بطبقة Win32 مصغّرة)

- `Utf8Stream.h` مع ASan/UBSan: عربي، CRLF، ملف قديم Latin-1، BOM، ملف مفقود/فارغ، كتابة كبيرة تتجاوز حد التفريغ، `rdbuf()`.
- محلل JSON: حالات الأخطاء المعروفة + 200 ألف مدخل عشوائي بدون تعطل + مقارنة بالقديم في 2.4 مليون فحص على رسائل سليمة: **صفر فروق**.
- JS: صياغة سليمة، ترجمة 273/273، بلا مفاتيح مفقودة، ولا IDs مكررة، وأسماء ألعاب صعبة تمر بدون حقن.

## قائمة اختبار بعد البناء (على Windows)

1. المشروع ينبني بدون أخطاء (Release و Debug).
2. أضف لعبة باسم عربي ومسار فيه عربي → أعد التشغيل → تبقى في القائمة و`games.cfg` سليم.
3. أضف لعبة اسمها فيه `'` (مثل Assassin's Creed) → زر الحذف يعمل.
4. اسحب مجلداً/ملفات مساراتها فيها `[ ]`.
5. تصدير ثم استيراد نسخة احتياطية فيها أسماء عربية.
6. افتح لوحة التحكم: الشكل مطابق للسابق، والدائرة تشتغل بالاختصار.
7. شغّل لعبة بأولوية High + Boost (كمسؤول) → بعد الإغلاق تُسترجع قيم MMCSS/SystemResponsiveness.
8. جرّب تشغيل لعبة مسارها محذوف → لا يزيد عدد مرات اللعب.
9. Black Box بدون صلاحيات مسؤول → لا تأخير ملحوظ عند التشغيل.
10. اسحب مجلداً فيه ملف تشغيل واحد → يظهر حوار الاقتراح بنص صحيح (لا `suggestExeMessage`).
11. زر "استرجاع إعدادات Boost": يعطي رسالة نتيجة واضحة.
12. شريط عنوان اللوحة داكن.
13. (اختياري) سكربت `tools\build-ui-assets.ps1` ثم افصل الإنترنت وافتح اللوحة.
14. (اختياري) بناء المثبّت وتجربته: تثبيت / ترقية / إزالة.
15. زر "استيراد الألعاب": تظهر ألعاب Steam/Epic/GOG/Ubisoft المثبّتة، وتُضاف الاختيارات وتشتغل من الدائرة.
16. أفلت مجلد `steamapps\common` (أو Steam كله): تظهر ألعاب كل المجلدات الفرعية بأسماء المجلدات.
17. Chrome / Edge / Epic Launcher تفتح من الدائرة (خصوصاً على جهاز صديقك).
18. تثبيت جديد (احذف مجلد `%APPDATA%\GameLauncher` للتجربة): تفتح اللوحة والدائرة مرة واحدة فقط.
19. صدّر نسخة احتياطية، احذف لعبة، استورد: الإحصائيات وسجل الجلسات ترجع.
20. مرّر الماوس على أيقونات صف اللعبة وشاراتها: يظهر شرح لكل واحدة.
21. لعبة بلا برنامج مرافق: شريط "الوصول السريع" مخفي؛ أضف مرافقاً فيظهر.
22. أضف صورة لخلفية دائرية جديدة: "إخفاء الأيقونة الأصلية" مفعّل تلقائياً؛ أطفِه وأضف صورة ثانية: يبقى مطفياً.
23. "استيراد الألعاب" مع وجود `D:\Games` (أو `C:\Games`): تظهر ألعابه بشارة Games.
24. مسح `%APPDATA%\GameLauncher` وتشغيل البرنامج: اللغة إنجليزية، وتفتح اللوحة والدائرة. ثم أعد التشغيل: تبقى إنجليزية.
25. أيقونة البرنامج في التراي وشريط المهام وملف الـ exe وقائمة ابدأ (بعد التثبيت): واضحة وليست ضبابية.
